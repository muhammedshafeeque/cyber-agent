// Note: Containerization removed per requirements
// This file kept for potential future use or tool execution isolation needs

// Direct tool execution on host system
// All tools run directly without Docker containers

module.exports = {
  // Tool execution mode: 'direct' (no containers)
  executionMode: 'direct',
};

